<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Chat - <%= user.username %></title>
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
  <link rel="stylesheet" href="/css/chatstyles.css">
</head>
<body>
  <header class="bg-primary text-white py-2 mb-4">
    <div class="container">
      <nav class="navbar navbar-expand-lg navbar-dark">
        <a class="navbar-brand" href="/">
          <i class="fas fa-comments"></i> Chat App
        </a>
        <div class="ml-auto d-flex align-items-center">
          <span class="mr-3"><%= user.username %></span>
          <a href="/auth/logout" class="btn btn-outline-light">Logout</a>
        </div>
      </nav>
    </div>
  </header>

  <main class="container-fluid">
    <div id="chatContainer" class="row d-flex">
      <aside id="onlineFriends" class="col-3 d-flex flex-column">
        <input type="text" id="searchBox" placeholder="Search..." class="form-control mb-3">
        <h4>Online Friends</h4>
        <ul id="onlineFriendsList" class="list-group">
          <!-- Online friends will be dynamically populated here -->
        </ul>
      </aside>

      <section id="chatMessages" class="border p-3 col-9">
        <!-- Chat messages will be dynamically populated here -->
      </section>
    </div>
  </main>

  <div id="messageInputContainer" class="mt-3">
    <form id="messageForm" class="d-flex">
      <input type="text" id="messageInput" class="form-control" placeholder="Type your message..." aria-label="Message input">
      <button type="submit" class="btn btn-primary ml-2" aria-label="Send message">
        <i class="fas fa-paper-plane"></i>
      </button>
    </form>
  </div>

  <script data-user-id="<%= user._id %>" ></script>
  <script src="https://code.jquery.com/jquery-3.5.1.min.js"></script>
  <script src="/socket.io/socket.io.js"></script>
  <script src="/js/chat.js"></script>
</body>
</html>
